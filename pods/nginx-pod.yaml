apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx-web
  annotations:
    description: "Nginx pod for learning purposes"
spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 8081
