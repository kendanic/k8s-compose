apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx
spec:
  type: NodePort
  ports:
    - port: 8001
      protocol: TCP
      targetPort: nginx-srv-port
  selector:
    app: nginx